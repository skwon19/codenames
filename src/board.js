class Board {

  /**
   * Codenames Board Constructor
   * /
  constructor(){
  }

  /**
   * Flip a card's status.
   * If this operation changes the status of a card, return true
   * Otherwise, return false
   */
  flipCard(row, col) {
    return false;
  }

  /**
   * Return the current status of the board
   */
  getBoard() {
    return null;
  }

  /**
   * Print the Board's state as a string
   */
  printBoard() {
    return "";
  }

}
